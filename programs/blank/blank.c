#include "simpos.h"
#include "stdlib.h"
#include "stdio.h"
#include "string.h"

int main(int argc, char **argv)
{
    /* prints out the arguments as a test */
    for(int i = 0; i<argc; i++)
    {
        printf("%s \n", argv[i]);
    }

    while (1)
    {
    }

    return 0;
}